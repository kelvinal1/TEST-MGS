import { InvoiceGeneralDTO } from './invoice-general-dto';

describe('InvoiceGeneralDTO', () => {
  it('should create an instance', () => {
    expect(new InvoiceGeneralDTO()).toBeTruthy();
  });
});
